# benchmark corpora, built binaries, patches: see bench/.gitignore (only logs/ and results/ are kept)

# prototype build outputs
bench/gotransform/gotransform
bench/gotransform/gotransform-*
bench/testsrv/testsrv
bench/goprobe/goprobe
bench/goattr/goattr
*.test

# editor / local agent settings
.idea/
.claude/settings.local.json

# compiled benchmark harness
bench/elfpredict/elfpredict
